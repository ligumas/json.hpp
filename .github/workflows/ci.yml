name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: build and test
        run: g++ -std=c++17 -O2 -Wall -o test test.cpp && ./test
